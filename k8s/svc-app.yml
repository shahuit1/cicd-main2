apiVersion: v1
kind: Service
metadata:
  name: businesslogodesign-web
  namespace: bld
spec:
  selector:
    app: businesslogodesign-web
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 80